enum UserRole {
  USER
  ADMIN
}

model User {
  id String @id @default(uuid())

  // ============================================================
  // ACCOUNT & PROFILE
  // ============================================================
  email  String   @unique
  role   UserRole @default(USER)
  name   String?
  avatar String?

  @@index([email])
  @@map("users")
}
