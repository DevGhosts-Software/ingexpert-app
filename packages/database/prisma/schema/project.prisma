model Project {
  id      String @id @default(uuid())
  name    String @map("nombre")
  contact String @map("contacto")
  address String @map("direccion")
  manager String @map("encargado")

  movements Movement[]

  @@map("projects")
}
